apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wayfarer-posts-ingress
  namespace: wayfarer
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wayfarer-posts-service
            port:
              number: 80
